%!
% PDFA_def.ps - Corrected for modern Ghostscript versions

% Define the path to the ICC Profile.
% Make sure 'sRGB.icc' is in the same directory or provide a full path.
/ICCProfile (sRGB.icc) def

% Create the OutputIntent dictionary
<<
  /Type /OutputIntent
  /S /GTS_PDFA1
  /OutputConditionIdentifier (sRGB IEC61966-2.1)
  /Info (sRGB IEC61966-2.1)
  /RegistryName (http://www.color.org)
  /DestOutputProfile (%pipe%ICCProfile) % Read the profile file
>>
setpagedevice