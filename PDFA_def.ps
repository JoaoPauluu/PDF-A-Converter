%!
% PDFA_def.ps - Corrected for Adobe RGB (1998)

% Define the path to the ICC Profile.
% Make sure 'AdobeRGB1998.icc' is in the same directory or provide a full path.
/ICCProfile (AdobeRGB1998.icc) def

% Create the OutputIntent dictionary
<<
  /Type /OutputIntent
  /S /GTS_PDFA1
  /OutputConditionIdentifier (Adobe RGB (1998))
  /Info (Adobe RGB (1998))
  /RegistryName (http://www.color.org)
  /DestOutputProfile (%pipe%ICCProfile) % Read the profile file
>>
setpagedevice